# The Perfect Guess

This is a simple Python guessing game.

- The program generates a random number.
- The user guesses the number.
- It tells "Higher" or "Lower" until correct.
- Finally, it shows how many attempts were used.

Run using:
python main.py
